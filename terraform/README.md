Terraform skeleton:
- Fill provider credentials and variables for your cloud.
- Use the KMS / HSM instructions in docs/key_management.md to create keys and bind to service accounts.
- After creating cluster, apply Kubernetes manifests from k8s/.
